@model RegisterEmployeeVM
@{
    Layout = null;
}
@*<div class="card mt-5">
    <form method="POST" >
    <div asp-validation-summary="All"></div>
    <div class="form-group">
    <label asp-for="Password" class="form-label"><i class="fas fa-key"></i></label>
    <input asp-for="Password" class="form-control"  />
    </div>
    <div class="form-group">
    <label asp-for="AgainPassword" class="form-label"><i class="fas fa-key"></i></label>
    <input asp-for="AgainPassword" class="form-control"/>
    </div>

    <div class="form-group form-button">
    <input type="submit" asp-route-id="@Model.ID" />
    </div>

    </form>
    </div>*@

    <head>
        <link href="~/Boostrap5.1.3/css/bootstrap.min.css" rel="stylesheet" />
    </head>

    <div class="d-flex justify-content-center">
        <div>
            <div class="card-body pt-10">
                <div class=" card-header fs-4  text-center">Şifrenizi Oluşturun </div>
                <div class="card-body">
                    <form method="post">

                        <div class="form-group">
                            <label asp-for="Password" class="from-label pt-2"></label>
                            <input id="pass" asp-for="Password" type="password" pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)[a-zA-Z\d]{8,}$" title="En az biri rakam, biri büyük ve biri küçük harf olmak üzere  8 karakter" />
                     

                        </div>

                        <div class="form-group mb-3">
                            <i class="fas fa-key"></i>
                            <label asp-for="AgainPassword" class="from-label pt-2"></label>
                            <input id="againpass" asp-for="AgainPassword" type="password" pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)[a-zA-Z\d]{8,}$" title="En az biri rakam, biri büyük ve biri küçük harf olmak üzere  8 karakter" />
                        </div>


                        <div class="text-center">
                            <button type="submit" asp-route-id="@Model.ID" class="btn btn-primary "> Kaydet  </button>
                        </div>

                    </form>
                </div>

            </div>

        </div>

    </div>
  

    